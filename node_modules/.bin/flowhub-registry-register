#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../flowhub-registry/bin/flowhub-registry-register" "$@"
  ret=$?
else 
  node  "$basedir/../flowhub-registry/bin/flowhub-registry-register" "$@"
  ret=$?
fi
exit $ret
